# iCommunicate | CARE Hospitals — local copy

This package was reconstructed from the supplied saved webpage snapshot.

Files:
- index.html — webpage structure/content
- style.css — extracted stylesheet

The snapshot contains the CARE logo as an embedded image, so no separate logo file is required.

Note: this is a static copy of the supplied snapshot. Any live Google Apps Script, API, CRM, login, database, or other server-side functionality is not recreated by the snapshot itself.
